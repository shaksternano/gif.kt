package com.shakster.gifkt.gradle

import org.gradle.api.Plugin
import org.gradle.api.Project

// Needed to register a plugin
@Suppress("unused")
class GifKtPlugin : Plugin<Project> {

    override fun apply(project: Project) = Unit
}
